# __init__.py — Makes "utils" a Python package so we can import from it.
#
# This allows:
#   from utils.pipefy_client import PipefyClient
#
# Without this file, Python wouldn't recognize "utils" as a package.
